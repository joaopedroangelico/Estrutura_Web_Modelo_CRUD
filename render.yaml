services:
  - type: web
    name: mecanica-backend
    runtime: node
    rootDir: mecanica-backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 3001
